// Quick test to verify our type fixes
import { NetworkConfig, NetworkConfigSchema } from './src/types/index.js';

// Test that our schema now includes lcdUrl
const testConfig: NetworkConfig = {
  chainId: "secret-4",
  name: "Secret Network Mainnet",
  rpcUrl: "https://secretnetwork-rpc.lavenderfive.com:443",
  lcdUrl: "https://lcd.mainnet.secretsaturn.net", // This should now work
  grpcUrl: "https://secretnetwork-grpc.lavenderfive.com:443",
  nativeDenom: "uscrt",
  coinGeckoId: "secret",
  explorer: "https://secretnodes.com"
};

// Test schema validation
const result = NetworkConfigSchema.safeParse(testConfig);
console.log('Schema validation result:', result.success);

// Test that lcdUrl is accessible
console.log('lcdUrl accessible:', testConfig.lcdUrl);

export {};
